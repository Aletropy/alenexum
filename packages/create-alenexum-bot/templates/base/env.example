# Copy to .env — never commit real tokens.
DISCORD_TOKEN=
# Guild-scoped dev deployment (instant). Leave empty to skip deployment.
GUILD_ID=
# guild | global | skip (default: guild when GUILD_ID is set, otherwise skip)
DEPLOY_MODE=
# debug | info | warn | error
LOG_LEVEL=info
